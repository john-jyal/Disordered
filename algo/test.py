def solution(money):
    answer = 0
    odd = 0
    even = 0
    if len(money) % 2 == 0
        for i, mon in enumerate(money):
            if i % 2 != 0:
                odd += mon
            else:
                even += mon
    else:

    answer = max(odd, even)
    return answer